import { CargarAfiliadoDirective } from './cargar-afiliado.directive';

describe('CargarAfiliadoDirective', () => {
  it('should create an instance', () => {
    const directive = new CargarAfiliadoDirective();
    expect(directive).toBeTruthy();
  });
});
